<%# Rails flash messages styled for Twitter Bootstrap 3.0 %>
<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
    <div style="display: none" id="flash-notice" data-name="<%=name%>" data-msg="<%=msg%>"></div>
  <% end %>
<% end %>
